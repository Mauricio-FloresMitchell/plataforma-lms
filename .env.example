# URL del Web App de Apps Script del Generador de Matrículas.
# Se obtiene al publicar el proyecto de Apps Script como
# Implementar > Nueva implementación > Aplicación web (termina en /exec).
# Copia este archivo a .env.local y pega tu URL real; .env.local no se sube al repositorio.
VITE_APPS_SCRIPT_URL=
