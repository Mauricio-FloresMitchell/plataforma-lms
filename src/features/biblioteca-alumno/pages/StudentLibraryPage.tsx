import { LibraryReadOnlyView } from '@/components/LibraryReadOnlyView'

/** Biblioteca / Recursos del Alumno (Sprint 16, Parte 6/7). */
export function StudentLibraryPage() {
  return <LibraryReadOnlyView homeTo="/alumno" />
}
