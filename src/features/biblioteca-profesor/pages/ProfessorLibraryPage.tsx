import { LibraryReadOnlyView } from '@/components/LibraryReadOnlyView'

/** Biblioteca / Recursos del Profesor (Sprint 17, Parte 13): solo lectura, descarga y consulta. */
export function ProfessorLibraryPage() {
  return <LibraryReadOnlyView homeTo="/profesor" />
}
